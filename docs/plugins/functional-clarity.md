# functional-clarity

Принципы кода в работе агента: хук вносит короткую выжимку в каждую сессию, скилл
раскрывает их целиком. Файлов не пишет.

## Состав

| Компонент | Что делает |
|---|---|
| хук `SessionStart` | вносит в сессию fail-fast, запрет скрытия ошибок, минимальные изменения, явные ошибки, стиль комментариев |
| скилл `functional-clarity` | принципы кода целиком, стиль программирования, дисциплина изменения существующего кода, словарь границ контекста |

## Как пользоваться

### Когда включается скилл

Скилл подключается сам, когда речь о принципах Функциональной ясности: как
поступать с ошибками, стоит ли скрывать сбой, как переработать существующий код,
сходится ли решение с принятыми в проекте правилами. Точные пусковые формулировки
живут в самом скилле, страница их не повторяет.

### Дисциплина изменения существующего кода

1. Понять идею — зачем код написан так.
2. Поставить идею под сомнение первой.
3. Назвать свои допущения.
4. Проверить гипотезы запуском, а не чтением.
5. Спросить человека там, где неясно, как система должна себя вести.
6. Не менять контракт метода без обсуждения.
7. Не удалять информацию — заменять лучшей.

### Где лежат принципы и стиль целиком

Здесь их нет намеренно: перечень длинный и меняется вместе со скиллом, а вторая
копия на сайте разошлась бы с ним молча. И перечень принципов, и стиль
программирования поставляются вместе с плагином; точка входа —
`plugins/functional-clarity/skills/functional-clarity/SKILL.md`. В нём принципы
даны рабочей выжимкой, а полное изложение лежит в справочниках рядом:

- `references/00-principles.md` — весь перечень принципов с разбором и примерами
  кода: единственная задача функции и минимальные изменения, явные ошибки и
  зависимости, именование и работа с состоянием, проверка входа и целостность
  данных, порядок работы над задачей, запрет скрытия ошибок, таймауты внешних
  вызовов, умолчания по роли компонента;
- `references/01-style-guide.md` — стиль программирования: размер и назначение
  функции, свои классы исключений, stdlib по умолчанию, отделение публичного
  интерфейса от вспомогательного, обязательные таймауты;
- там же — дисциплина изменения чужого кода целиком, разбор комментариев, словарь
  границ контекста, уровни разработчика и особенности Python.

## Границы

- Разбора конкретного дифа со ссылками `файл:строка` плагин не делает: он даёт
  принципы и дисциплину, а не находки по одному изменению.
- Тестами он не занимается и цикла тестов не изображает.
- Предмет — код. Ясность человекочитаемых текстов и своды компетенций сюда не
  входят.

## Куда дальше

- [sdlc](sdlc.md) — ревью конкретного дифа со ссылками `файл:строка`; все три роли
  плагина опираются на эти принципы.
- [tdd-master](tdd-master.md) — цикл тестов до продуктового кода; когда цикл нужен,
  а плагин не установлен, скилл советует его поставить.
- [clarity-language](clarity-language.md) — ясность человекочитаемых текстов.
- [fpf-integration](fpf-integration.md) — своды принципов и компетенций FPF.
  Плагины независимы: FPF-скиллы из этого плагина не тянутся — если нужны и
  принципы кода, и FPF-экосистема, ставьте оба.
- [Связи между плагинами](relations.md) — чем именно связаны эти плагины и что
  ломается без второго.
