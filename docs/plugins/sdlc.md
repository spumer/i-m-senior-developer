# sdlc

Три роли разработки — проектирование, реализация, ревью. Каждая поставляется
парой: агент, которого вызывают, и скилл с методом этой роли.

## Состав

| Роль | Модель | Что ведёт |
|---|---|---|
| `architect` | opus | ограниченные контексты, договоры передачи, поток данных, точки интеграции, открытые вопросы |
| `code-implementer` | sonnet | реализацию по готовому документу через цикл тестов; запускает весь набор тестов и линтеры |
| `code-reviewer` | sonnet | безопасность, принципы ясности, стековые ловушки; замечания со ссылкой `файл:строка` |

Команд, хуков и workflow у плагина нет: порядок вызова задаёт человек или
оркестратор.

## Как пользоваться

### Передача между ролями

```text
требования → architect → code-implementer → code-reviewer
```

- Тесты и линтеры запускает `code-implementer` и отчитывается точными командами.
- `code-reviewer` читает диф и этот отчёт; находку, требующую запуска, помечает
  непроверенной.
- Дефект реализации возвращает работу реализующей роли. Замечание к
  проектированию оркестратор показывает человеку и останавливает выполнение до
  следующей рабочей фазы. Архитектор сам не вызывается; если человек меняет
  архитектуру, гейт свежести требует пересобрать план выполнения.

### Артефакты

```text
<каталог фичи>/ARCHITECTURE.md                      architect
<каталог фичи>/IMPLEMENTATION-NN.md                 code-implementer, один файл на реализацию или круг правок
<каталог фичи>/review-request-changes/REVIEW-NN.md  code-reviewer, один файл на раунд
```

Путь архитектурного документа архитектор берёт по порядку: сначала названный
оркестратором (в конвейере planner — из шапки `PLANNER_EXECUTION.md`), затем
соглашение проекта из `planner-context.md`, затем `ARCH-NN.md`.

При самостоятельном вызове реализатор и ревьюер выводят полный отчёт в чат. При
вызове из оркестратора полный отчёт записывается в переданный файл, а в его
контекст возвращается доказательная сводка.

### Стековые справочники

| Роль | Всегда | По стеку |
|---|---|---|
| `architect` | договоры проектирования | Python, React, дизайн API |
| `code-implementer` | — | Python, React |
| `code-reviewer` | безопасность (OWASP) | Python, React |

Признаки стека: `*.py` даёт Python; `*.tsx`, `*.jsx`, `*.ts`, `*.vue`,
`*.svelte` — фронтенд; поверхность API добавляет справочник по дизайну API;
смешанный стек берёт оба справочника, сначала бэкенд; на неизвестном стеке
остаются универсальные принципы, отказа нет.

Проход по безопасности выполняется всегда и первым; раздел о безопасности
присутствует в отчёте, даже когда находок нет.

Тексты справочников поставляются вместе со скиллами ролей и лежат в
`plugins/sdlc/skills/<роль>/references/`; страница называет только их назначение.

### Зависимости

Плагин объявляет зависимость от `tdd-master` и `functional-clarity`, и установка
подтягивает их сама. Без `tdd-master` цикл тестов допустим общими средствами, но
работа в этом случае идёт в ослабленном виде, и это нужно отметить в отчёте.

Необязательная интеграция — `document-skills:frontend-design`: архитектор
называет дизайн-систему в документе, реализатор включает её для визуального
качества компонентов, ревьюер — для разбора визуальных изменений фронтенда.
Без этого плагина все три роли продолжают работать, деградации отказа нет.

## Границы

- Цикл сам не запускается: у плагина нет ни команд, ни хуков.
- Ради однострочной правки роли не зовут: три передачи между ролями стоят
  дороже, чем правка. Роли берут, когда у изменения есть контракт, граница или
  поверхность безопасности.
- `architect` работает только по готовым требованиям фичи и сам их не собирает.
- `architect` не пишет код, тесты и миграции и не запускает тесты.
- `code-implementer` не проектирует и не проводит ревью.
- `code-reviewer` не правит код, не пишет тесты и не запускает набор тестов: он
  ставит диагноз, правки делает реализующая роль.

## Куда дальше

- Сырой замысел, требования фичи и оркестровка фаз с отчётами —
  [`planner`](planner.md); он же держит гейт свежести плана.
- Цикл тестов, который включает реализующая роль —
  [`tdd-master`](tdd-master.md); принципы кода —
  [`functional-clarity`](functional-clarity.md).
- Полный перечень связей, зависимостей и смежных проектов —
  [связи между плагинами](relations.md).
