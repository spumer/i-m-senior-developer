# Worked-evidence — журнал применений (E.4.DPF.DA, D8)

- 2026-07-13/14 · f471117, 748a4cd, 9fa714c (стадии «Упрощение» этапов 1–3 конвейера 4.0) · ground (через apply LPF-SIMPLIFICATION-REVIEW) · П1 «Role-first» + П2 «connascence» + каталог №8/№11/№12 · три боевых прогона: П1 удержал от слияния kombu↔faststream-симметрий (Случай B §10 воспроизвёлся на живых кандидатах — sync/async runtime-модели), охота №8 дала 7 принятых реальных обобщений без единого пере-обобщения №12 · исход: подтверждает
- 2026-07-14 · 3489692 · ground · П5 «Bounded-context guard» · принятые обобщения легли строго по границе (миксины внутри бэкенд-поддеревьев, один core-примитив, направление backends→core) — основание «поверхность и опоры» работало как операционный критерий · исход: подтверждает
- 2026-07-16 · T0 FEAT-0001-django-box (`features/FEAT-0001-django-box/FEAT-0001-T0-box-decoupling.md`) · ground · П1–П6 в ОБРАТНОМ направлении (decoupling, PUR-3) · карта исключаемости box из релиза 4.0: 13 групп точек, 100% grep-покрытие, evidence-прогоны (v3.4.0 без box; 439 passed без box-тестов; hard-block django) → рекомендация experimental по П6; Owner выбрал физическое вырезание (приоритет чистоты релиза перевесил П6-рентабельность — прогнозные входы взвешивает владелец), исполнено: 400 passed / mypy / ruff / Sphinx-паритет · исход: подтверждает применимость свода в обратном направлении; показал границу — П3-критерий при decoupling читается «рёбра через границу исчезли/ослабли», не «число сократилось» (кандидат в правку свода владельцу)
- 2026-07-20 · разбор двух кандидатов на упрощение интерфейсов (диалог с мейнтейнером, дифф не создавался): `core/queue_manager.py` (иерархия) и `backends/faststream/middlewares.py::_publish_pending_response` (noop-хвост) · ground · П1–П6 в обратном направлении (PUR-3) + П1 «Role-first» на noop-кандидате · LSP-карта ссылок 100% (12 имён, no silent tail): иерархия подтверждена как Shared Kernel уровня интерфейса (Случай A §10), «много классов» = матрица ролей 4 направления × sync/async (Случай B); найден реальный хвост — 6 абстрактных `_build_headers` без единого cross-backend/полиморфного потребителя (connascence of name, не роль); noop-кандидат — дефект имени, не дефолта: NotImplementedError в базе был бы пойман `except Exception` в `__aexit__` и превращён в RejectMessage на успешном пути event_in/response_in (предсказание по трассе кода, тестом не прогонялось) · исход: подтверждает (П1 остановил и наивное «слить классы», и наивное «NotImplementedError по умолчанию»)
- 2026-07-15 · разбор узла «публикация» (диалог с мейнтейнером, дифф PublishOptions незакоммичен) · ground · П1–П6 по PUR-1, вход — evidence из git-истории (коммит 1ec5438: одна опция = 5 файлов/+322 строки; 86 упоминаний acquire_timeout) · три альтернативы Boundary-уровня поданы мейнтейнеру с графами до/после, приняты A (параметр-объект политики) и B (билдер отдельно от доставки), C (тонкий фасад) отклонён владельцем · исход: подтверждает (П3 «граф, не эстетика» + П6 «волатильность как факт из истории, не гипотеза» дали решению основание, которое мейнтейнер смог оценить по существу)
