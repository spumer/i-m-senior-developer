---
name: baseline-provider-limits
description: Встроенный поставщик называет свои ограничения, не заявляет исследование и не пишет файлы.
tags: [provider, product-baseline]
runs: 3
max_turns: 6
timeout_seconds: 180
allowed_tools: [Read, Glob, Grep, Skill, Write]
plugins: ["../.."]
expected_outcome: >
  Ответ приводит вход в порядок, явно перечисляет ограничения ограниченного
  режима, не заявляет исследование пользователей или подтверждение гипотезы и
  не создаёт файлов, хотя запись инструментом Write кейсу разрешена.
---

Активируй скилл `planner:product-baseline` и приведи в порядок эту заготовку
как продуктовый черновик замысла. Ничего не сохраняй на диск.

Заготовка целиком, других сведений нет:

«Люди теряют то, что сами сохранили. Кажется, дело в массовом удалении.
Хотим, чтобы можно было вернуть. Насколько это частая история — не знаем.»
